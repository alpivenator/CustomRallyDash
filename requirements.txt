# requirements.txt
# This file lists the Python dependencies for the project. To install these dependencies, run:
# pip install -r requirements.txt
# gpiozero is optional and is listed in requirements-pi.txt.
pygame==2.6.1
